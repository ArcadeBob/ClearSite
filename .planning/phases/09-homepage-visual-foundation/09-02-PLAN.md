---
phase: 09-homepage-visual-foundation
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/pages/HomePage.tsx
  - src/components/CertificationsBadges.tsx
  - src/components/GCResourcesSection.tsx
  - src/components/ServiceCard.tsx
  - src/components/GCPainPoints.tsx
  - src/components/ProcessTimeline.tsx
  - src/components/PromiseSection.tsx
  - src/components/PrevailingWageBanner.tsx
  - src/components/ProjectCard.tsx
autonomous: true
requirements: [HOME-02, TYPO-01]

must_haves:
  truths:
    - "Prequal cards and certifications appear as a single compact band on the homepage, not two separate full-height sections"
    - "Card titles and smaller headings (h3, h4) render in the body sans-serif font, not Bebas Neue"
    - "Hero titles and major section headers (h1, h2) still use Bebas Neue"
  artifacts:
    - path: "src/pages/HomePage.tsx"
      provides: "Consolidated trust band replacing separate GCResourcesSection and CertificationsBadges"
    - path: "src/components/CertificationsBadges.tsx"
      provides: "Compact certifications display suitable for inline band"
    - path: "src/components/ServiceCard.tsx"
      provides: "Card title without font-heading class"
    - path: "src/components/GCPainPoints.tsx"
      provides: "Pain point card titles without font-heading class"
    - path: "src/components/ProcessTimeline.tsx"
      provides: "Step titles without font-heading class"
    - path: "src/components/PromiseSection.tsx"
      provides: "Promise card titles without font-heading class"
  key_links:
    - from: "src/pages/HomePage.tsx"
      to: "CertificationsBadges"
      via: "consolidated trust band section"
      pattern: "CertificationsBadges|GCResourcesSection"
    - from: "src/components/ServiceCard.tsx"
      to: "Tailwind classes"
      via: "font-bold without font-heading"
      pattern: "text-xl font-bold"
---

<objective>
Consolidate the prequal cards and certifications into one compact trust band on the homepage, and restrict Bebas Neue (font-heading) to hero titles and major section headers only — removing it from card titles and smaller headings across all components.

Purpose: The homepage currently has two separate full-height sections for prequal info (GCResourcesSection) and certifications (CertificationsBadges), making the page too long. Card titles using Bebas Neue create visual noise — the display font should be reserved for impactful hero/section headers.

Output: A more compact homepage with a single trust band section, and consistent typography where only major headers use Bebas Neue while card/sub-headings use the default sans-serif.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-homepage-visual-foundation/09-01-SUMMARY.md
@src/pages/HomePage.tsx
@src/components/CertificationsBadges.tsx
@src/components/GCResourcesSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate prequal cards and certifications into compact trust band</name>
  <files>src/pages/HomePage.tsx, src/components/CertificationsBadges.tsx</files>
  <action>
**HOME-02: Merge GCResourcesSection and CertificationsBadges into one compact section on the homepage.**

In HomePage.tsx, replace the two separate sections:
```
<GCResourcesSection />
<CertificationsBadges />
```

with a single inline section that combines the certification badges directly below/alongside the GC Resources content. Two approaches (choose the simpler one):

**Approach — Embed certifications inside GCResourcesSection area:**
1. Remove the standalone `<CertificationsBadges />` call from HomePage.tsx
2. In the HomePage section where `<GCResourcesSection />` is rendered, add the certification badges as a compact horizontal row directly below it within the same wrapping section (no extra py-24 padding). The certifications should appear as a row of 4 small badge cards (icon + title + subtitle) in a single horizontal strip with minimal padding (py-6 to py-8), using the same bg-brand background as GCResourcesSection so they visually merge.

Specifically in HomePage.tsx:
- Keep `<GCResourcesSection />` as-is
- Below it (but within the same visual section), render a compact certification strip. Import the certification data from CertificationsBadges or inline the 4 badges.
- Remove the standalone `<CertificationsBadges />` line

**CertificationsBadges.tsx** — Export the `certifications` data array so it can be consumed by HomePage.tsx, OR create an alternate compact rendering mode via a `variant="compact"` prop that renders as a single-row horizontal strip with less padding (py-6 instead of py-20, no section header, just the 4 badges in a tight grid). If using the variant approach:
- `variant="full"` (default): Current full section layout
- `variant="compact"`: Single row, no section header, reduced padding, bg-brand text-white styling to match GCResourcesSection

The goal is that prequal info + certifications appear as ONE visual band, not two separate full-height sections. Total combined height should be roughly what one of them was before.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Homepage shows prequal info and certifications as a single visual band
- No standalone full-height CertificationsBadges section on homepage
- CertificationsBadges component still works for use on other pages if needed
- Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Restrict Bebas Neue to hero titles and major section headers only</name>
  <files>src/components/ServiceCard.tsx, src/components/GCPainPoints.tsx, src/components/ProcessTimeline.tsx, src/components/PromiseSection.tsx, src/components/GCResourcesSection.tsx, src/components/CertificationsBadges.tsx, src/components/PrevailingWageBanner.tsx, src/components/ProjectCard.tsx</files>
  <action>
**TYPO-01: Remove `font-heading uppercase tracking-wide` from h3/h4 card titles and smaller headings. Keep it ONLY on h1 hero titles and h2 major section headers.**

The rule: `font-heading` (Bebas Neue) stays on:
- Page hero h1 titles (HomePage, AboutPage, ProjectsPage, ContactPage, ResidentialPage, CaseStudyPage, NotFoundPage — do NOT touch these)
- Major section h2 headers (SectionHeader component, and any h2 in HomePage, PromiseSection h2, GCResourcesSection h2, ServiceAreaMap h2 — do NOT touch these)
- Footer h3 column headers (these are structural headers, keep them)

Remove `font-heading uppercase tracking-wide` from these h3/h4 CARD TITLES (replace with `font-semibold` or `font-bold` — keep existing text size and color):

1. **ServiceCard.tsx** line 40: `font-heading uppercase tracking-wide text-xl font-bold` → `text-xl font-bold`
2. **GCPainPoints.tsx** line 93: `font-heading uppercase tracking-wide text-lg font-bold text-brand` → `text-lg font-bold text-brand`
3. **ProcessTimeline.tsx** line 142: `font-heading uppercase tracking-wide text-lg font-bold text-brand` → `text-lg font-bold text-brand`
4. **ProcessTimeline.tsx** line 190: `font-heading uppercase tracking-wide text-lg font-bold text-brand` → `text-lg font-bold text-brand`
5. **PromiseSection.tsx** line 51: `font-heading uppercase tracking-wide text-lg font-bold text-white` → `text-lg font-bold text-white`
6. **GCResourcesSection.tsx** line 75: `font-heading uppercase tracking-wide text-lg font-bold text-white` → `text-lg font-bold text-white` (InfoCard h3)
7. **GCResourcesSection.tsx** line 161: `font-heading text-2xl font-bold text-white` → `text-2xl font-bold text-white` (quickFacts value)
8. **GCResourcesSection.tsx** line 185: `font-heading uppercase tracking-wide text-lg font-bold text-white` → `text-lg font-bold text-white` (bottom CTA h3)
9. **CertificationsBadges.tsx** line 61: `font-heading uppercase tracking-wide text-lg font-bold text-brand` → `text-lg font-bold text-brand`
10. **PrevailingWageBanner.tsx** line 60: `font-heading uppercase tracking-wide text-lg font-bold text-brand` → `text-lg font-bold text-brand`
11. **ProjectCard.tsx** line 127: `font-heading uppercase tracking-wide text-lg font-bold text-brand` → `text-lg font-bold text-brand` (front title)
12. **ProjectCard.tsx** line 172: `font-heading uppercase tracking-wide text-lg font-bold text-white` → `text-lg font-bold text-white` (back title)

Do NOT touch:
- Any h1 elements
- Any h2 section headers (SectionHeader.tsx, PromiseSection h2, GCResourcesSection h2, HomePage h2s)
- Footer h3 column headers
- ErrorBoundary h1
- NotFoundPage h1/h2
- SafetySection stat values (these are data display, not headings)
- ContactForm success h3 (this is a one-off heading)
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Card titles (ServiceCard, GCPainPoints, ProcessTimeline, PromiseSection, GCResourcesSection cards, CertificationsBadges, PrevailingWageBanner, ProjectCard) render in default sans-serif, not Bebas Neue
- Hero h1 titles and major h2 section headers still use font-heading (Bebas Neue)
- Build and lint pass
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npm run lint` passes
- Homepage shows one combined trust band instead of two separate sections
- Grep for `font-heading` in ServiceCard.tsx returns no results
- Grep for `font-heading` in GCPainPoints.tsx returns no results (card h3s removed, section h2 handled by SectionHeader)
- Grep for `font-heading` in SectionHeader.tsx still shows it (preserved for section headers)
- Grep for `font-heading` in HomePage.tsx h1 still shows it (preserved for hero)
</verification>

<success_criteria>
1. Prequal cards and certifications appear as one compact visual band on the homepage
2. Card titles across all components render in body sans-serif font
3. Hero titles and major section h2 headers still use Bebas Neue
4. Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-homepage-visual-foundation/09-02-SUMMARY.md`
</output>
