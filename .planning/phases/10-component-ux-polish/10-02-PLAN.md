---
phase: 10-component-ux-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/pages/ContactPage.tsx
  - src/components/FloatingCTA.tsx
  - src/pages/HomePage.tsx
  - src/pages/AboutPage.tsx
  - src/pages/ResidentialPage.tsx
  - src/pages/ProjectsPage.tsx
  - src/pages/CaseStudyPage.tsx
autonomous: true
requirements:
  - UX-02
  - UX-03

must_haves:
  truths:
    - "Contact page sidebar shows different content for commercial vs residential inquiry types"
    - "Commercial sidebar keeps prequal checklist; residential sidebar shows residential services list"
    - "FloatingCTA hides when an inline CTA button is visible in the viewport"
    - "FloatingCTA reappears when all inline CTAs scroll out of view"
    - "FloatingCTA does not appear at all on the Contact page"
    - "Manual dismiss (X button) overrides inline CTA visibility until scroll resets"
  artifacts:
    - path: "src/pages/ContactPage.tsx"
      provides: "Inquiry-type-aware sidebar with conditional content"
      contains: "inquiryType"
    - path: "src/components/FloatingCTA.tsx"
      provides: "IntersectionObserver-based smart hide behavior"
      contains: "IntersectionObserver"
  key_links:
    - from: "src/components/FloatingCTA.tsx"
      to: "inline CTA buttons"
      via: "IntersectionObserver on data-cta-inline elements"
      pattern: "data-cta-inline"
    - from: "src/pages/ContactPage.tsx"
      to: "inquiryType from useSearchParams"
      via: "conditional sidebar rendering"
      pattern: "inquiryType.*residential"
---

<objective>
Enhance the contact page sidebar with inquiry-type-aware content and make the FloatingCTA intelligently hide when inline CTAs are visible.

Purpose: The contact sidebar currently shows the same prequal-focused content regardless of inquiry type — residential visitors see irrelevant commercial info. The FloatingCTA competes with inline CTA buttons, creating visual noise.
Output: Conditional sidebar content, smart FloatingCTA hide/show behavior with data attributes on inline CTAs.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-component-ux-polish/10-CONTEXT.md

@src/pages/ContactPage.tsx
@src/components/FloatingCTA.tsx
@src/pages/HomePage.tsx
@src/pages/AboutPage.tsx
@src/pages/ResidentialPage.tsx

<interfaces>
<!-- FloatingCTA current state management -->
From src/components/FloatingCTA.tsx:
```typescript
const [isVisible, setIsVisible] = useState(false);
const [isDismissed, setIsDismissed] = useState(false);
const { pathname } = useLocation();
const isResidential = pathname === '/residential';
// Show logic: scrollY > 500 && !isDismissed
// Dismiss resets when scrollY < 100
```

<!-- ContactPage inquiry type derivation -->
From src/pages/ContactPage.tsx:
```typescript
const [searchParams] = useSearchParams();
const inquiryType: 'commercial' | 'residential' =
  searchParams.get('type') === 'residential' ? 'residential' : 'commercial';
```

<!-- Existing sidebar sections in ContactPage: Office Location, Contact Details, Business Info, "What's in the Prequal Packet" checklist, Map Placeholder -->

<!-- Inline CTA buttons to mark with data-cta-inline: -->
<!-- HomePage.tsx: two "Request Prequal Package" Link/Buttons (~line 287, ~line 494) -->
<!-- AboutPage.tsx: "Request Prequal Package" (~line 407) -->
<!-- ResidentialPage.tsx: "Request a Quote" (~line 223) -->
<!-- CaseStudyPage.tsx: "Request Prequal Package" (~line 537) -->
<!-- ProjectsPage.tsx: check for CTA buttons -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make contact page sidebar inquiry-type-aware</name>
  <files>src/pages/ContactPage.tsx</files>
  <action>
1. Keep the existing sidebar structure: Office Location card, Contact Details card, Business Info card remain unchanged for both inquiry types.

2. Make the "What's in the Prequal Packet" section conditional on `inquiryType`:

   **Commercial (default):** Keep the existing amber-50 prequal checklist exactly as-is (COI, EMR, References, Company Profile, Bonding & License).

   **Residential:** Replace the prequal checklist with a residential services card:
   - Same visual style (use `bg-stone-50` with `border-stone-200` to differentiate from commercial amber)
   - Title: "Our Residential Services" with a Home or Layers icon
   - List of residential services with checkmarks:
     - Shower enclosures (frameless and semi-frameless)
     - Custom mirrors and mirror walls
     - Glass railings and balustrades
     - Interior glass partitions
     - Glass shelving and tabletops
   - Small note at bottom: "All residential projects include free on-site measurement"

3. Optionally add a "Why Choose CGI" or response timeline section below the conditional block for commercial inquiries if it adds value without clutter (Claude's discretion). Keep it brief — 3-4 bullet points max. If it feels forced, skip it.

4. The Map Placeholder section at the bottom remains unchanged for both types.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Contact page sidebar shows prequal checklist for commercial visitors and residential services list for residential visitors. All other sidebar sections unchanged. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add FloatingCTA smart hide with IntersectionObserver</name>
  <files>src/components/FloatingCTA.tsx, src/pages/HomePage.tsx, src/pages/AboutPage.tsx, src/pages/ResidentialPage.tsx, src/pages/CaseStudyPage.tsx, src/pages/ProjectsPage.tsx</files>
  <action>
1. **Mark inline CTA buttons** — Add `data-cta-inline` attribute to all primary CTA buttons/links across pages that link to the contact page. These are the buttons the FloatingCTA duplicates:
   - HomePage.tsx: the "Request Prequal Package" buttons/links (there are two — in the hero CTA area and in the bottom CTA section)
   - AboutPage.tsx: the "Request Prequal Package" button/link
   - ResidentialPage.tsx: the "Request a Quote" button/link
   - CaseStudyPage.tsx: the "Request Prequal Package" button/link
   - ProjectsPage.tsx: check for any CTA buttons linking to contact, mark if present

   Add the attribute to the outermost clickable element (the `<Link>` wrapper or `<a>` tag, not the inner `<Button>`).

2. **Update FloatingCTA logic** — Replace the simple scroll-threshold show/hide with:

   a. **Hide on Contact page entirely:** Add `pathname === '/contact'` check. If on contact page, never show FloatingCTA. Return null or keep hidden.

   b. **IntersectionObserver for inline CTAs:**
      - On mount (and when pathname changes), query all `[data-cta-inline]` elements
      - Create an IntersectionObserver that tracks visibility of these elements
      - Use a `Set` or counter to track how many inline CTAs are currently visible
      - When ANY inline CTA is visible (intersecting), set a `ctaVisible` state to true
      - When NO inline CTAs are visible, set `ctaVisible` to false
      - Use reasonable threshold (e.g., `threshold: 0.5`) and no rootMargin (or small margin like `rootMargin: '0px 0px 80px 0px'` to hide slightly before the CTA reaches the FloatingCTA bar)

   c. **Combined visibility logic:**
      - `isVisible` = `scrollY > 500` AND `!isDismissed` AND `!ctaVisible` AND `pathname !== '/contact'`
      - Manual dismiss (`isDismissed`) takes priority — if user dismisses, stay hidden until scroll resets regardless of inline CTA state
      - The existing scroll-reset behavior (reset `isDismissed` when `scrollY < 100`) remains unchanged

   d. **Cleanup:** Disconnect observer on unmount and re-observe when pathname changes (page navigation means different inline CTAs).

3. The slide-down transition for hiding is already in place (`translate-y-full`). No animation changes needed.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>FloatingCTA hides when any inline CTA button is in the viewport, reappears when scrolled past. FloatingCTA never shows on Contact page. Manual dismiss still overrides until scroll reset. All inline CTA buttons across 5 pages marked with data-cta-inline. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Contact page with `?type=commercial` shows prequal checklist in sidebar
- Contact page with `?type=residential` shows residential services list in sidebar
- FloatingCTA hides when scrolling past an inline CTA button on homepage
- FloatingCTA reappears when inline CTA scrolls out of view
- FloatingCTA never appears on `/contact` page
- Manual dismiss (X button) keeps FloatingCTA hidden until scroll resets to top
</verification>

<success_criteria>
1. Contact sidebar content adapts based on inquiry type (commercial vs residential)
2. FloatingCTA intelligently hides when inline CTAs are visible in viewport
3. FloatingCTA hidden entirely on Contact page
4. Manual dismiss still works independently of smart hide
5. TypeScript compiles, build succeeds, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/10-component-ux-polish/10-02-SUMMARY.md`
</output>
