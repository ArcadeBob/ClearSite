---
phase: 10-component-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ServiceCard.tsx
  - src/pages/HomePage.tsx
  - src/components/ProcessTimeline.tsx
autonomous: true
requirements:
  - VIZZ-02
  - UX-01

must_haves:
  truths:
    - "Each service card displays a thumbnail image strip above the icon/title area"
    - "Process timeline section has reduced vertical padding with tighter mobile step spacing"
  artifacts:
    - path: "src/components/ServiceCard.tsx"
      provides: "ServiceCard with image prop and top banner image strip"
      contains: "image"
    - path: "src/pages/HomePage.tsx"
      provides: "Services array with Unsplash image URLs for all 8 service types"
      contains: "unsplash"
    - path: "src/components/ProcessTimeline.tsx"
      provides: "Tighter section padding and mobile spacing"
      contains: "py-16"
  key_links:
    - from: "src/pages/HomePage.tsx"
      to: "src/components/ServiceCard.tsx"
      via: "image prop in services array spread to ServiceCard"
      pattern: "image.*unsplash"
---

<objective>
Add thumbnail images to service cards and reduce process timeline dead space.

Purpose: Service cards currently show only icons — adding a short image strip gives visual context for each glazing service. The process timeline has excessive padding that wastes vertical space.
Output: ServiceCard with image banner prop, 8 Unsplash images selected, tighter timeline spacing.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-component-ux-polish/10-CONTEXT.md

@src/components/ServiceCard.tsx
@src/pages/HomePage.tsx
@src/components/ProcessTimeline.tsx

<interfaces>
<!-- ServiceCard current interface -->
From src/components/ServiceCard.tsx:
```typescript
interface ServiceCardProps {
  title: string;
  description: string;
  icon: React.ReactNode;
  bullets?: string[];
}
```

<!-- Services array structure in HomePage (8 items with title, description, icon, bullets) -->
<!-- Each service object is spread into ServiceCard: <ServiceCard key={service.title} {...service} /> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add image prop to ServiceCard with top banner strip</name>
  <files>src/components/ServiceCard.tsx, src/pages/HomePage.tsx</files>
  <action>
1. In ServiceCard.tsx, add optional `image` prop to ServiceCardProps interface:
   - `image?: string` (URL for the banner image)

2. Add a top banner image strip ABOVE the existing `p-6` content area, inside the outer card div but before the content div:
   - Height: `h-28` (~112px) for a short visual accent
   - Use `object-cover` for the image
   - Add a subtle gradient overlay at the bottom edge using `bg-gradient-to-t from-white/80 to-transparent` (about `h-6` tall, absolutely positioned at the bottom of the image strip) to tie image to card content
   - Only render the image strip when `image` prop is provided (backward compat)

3. In HomePage.tsx, add an `image` field to each of the 8 service objects in the `services` array. Select appropriate Unsplash stock photos:
   - Storefronts: commercial glass storefront/entrance — e.g. `https://images.unsplash.com/photo-1497366216548-37526070297c?auto=format&fit=crop&w=600&q=80`
   - Window Walls: modern building with large glass windows — e.g. `https://images.unsplash.com/photo-1545558014-8692077e9b5c?auto=format&fit=crop&w=600&q=80`
   - Curtain Walls: glass curtain wall facade — e.g. `https://images.unsplash.com/photo-1486325212027-8081e485255e?auto=format&fit=crop&w=600&q=80`
   - Skylights: skylight or glass ceiling — e.g. `https://images.unsplash.com/photo-1494522855154-9297ac14b55f?auto=format&fit=crop&w=600&q=80`
   - Glass Railings: glass balustrade/railing — e.g. `https://images.unsplash.com/photo-1600585154340-be6161a56a0c?auto=format&fit=crop&w=600&q=80`
   - Fire-Rated Glazing: commercial interior glass partition — e.g. `https://images.unsplash.com/photo-1497366811353-6870744d04b2?auto=format&fit=crop&w=600&q=80`
   - Shower Enclosures: glass shower enclosure — e.g. `https://images.unsplash.com/photo-1552321554-5fefe8c9ef14?auto=format&fit=crop&w=600&q=80`
   - Mirrors: decorative mirror or mirror wall — e.g. `https://images.unsplash.com/photo-1618220179428-22790b461013?auto=format&fit=crop&w=600&q=80`

   Use Claude's discretion to pick the best image for each — the URLs above are suggestions. The key is that each image clearly relates to that service type and looks professional. Use `&w=600&q=80` params for reasonable size.

4. Ensure the card's `overflow-hidden` on the outer div clips the image correctly within the rounded corners.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 8 service cards display a ~112px image banner at the top. Image prop is optional for backward compat. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce process timeline section padding and mobile step spacing</name>
  <files>src/components/ProcessTimeline.tsx</files>
  <action>
1. Reduce section padding from `py-24` to `py-16` (or `py-12` if py-16 still feels spacious — use judgment).

2. Reduce mobile step spacing from `space-y-8` to `space-y-5`.

3. Optionally reduce the SectionHeader bottom margin from `mb-16` to `mb-12` if the overall section still feels too tall after the padding change.

4. Do NOT change:
   - Desktop 7-column grid layout
   - Icon sizes (96px desktop, 64px mobile)
   - Header text or internal step layout
   - Animation timing or behavior
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Process timeline section is visually more compact. Section padding reduced, mobile steps closer together. No layout breakage on desktop or mobile. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Dev server shows service cards with image banners above icon/title area
- Process timeline section is noticeably tighter with less whitespace
- Service cards without image prop still render correctly (backward compat)
</verification>

<success_criteria>
1. Each of the 8 service cards displays a thumbnail image relevant to its service type
2. Process timeline has reduced padding and tighter mobile spacing
3. TypeScript compiles, build succeeds, no visual regressions
</success_criteria>

<output>
After completion, create `.planning/phases/10-component-ux-polish/10-01-SUMMARY.md`
</output>
