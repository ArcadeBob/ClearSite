---
phase: 05-cta-simplification
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/pages/HomePage.tsx
  - src/pages/AboutPage.tsx
  - src/pages/CaseStudyPage.tsx
  - src/pages/ResidentialPage.tsx
  - src/components/FloatingCTA.tsx
  - src/components/GCResourcesSection.tsx
autonomous: true
requirements:
  - CTA-01
  - CTA-02

must_haves:
  truths:
    - "Every commercial CTA button linking to /contact includes ?type=commercial in the URL"
    - "Every commercial CTA primary button reads 'Request Prequal Package'"
    - "The residential CTA on ResidentialPage links to /contact?type=residential"
    - "The residential CTA reads 'Request a Quote'"
    - "FloatingCTA button text reads 'Request Prequal Package' (not 'Download Prequal Package')"
    - "AboutPage bottom CTA reads 'Request Prequal Package' (not 'Get a Free Consultation')"
  artifacts:
    - path: "src/pages/HomePage.tsx"
      provides: "Two commercial CTAs with ?type=commercial"
      contains: "?type=commercial"
    - path: "src/pages/AboutPage.tsx"
      provides: "Two commercial CTAs with ?type=commercial, corrected button text"
      contains: "?type=commercial"
    - path: "src/pages/CaseStudyPage.tsx"
      provides: "One commercial CTA with ?type=commercial"
      contains: "?type=commercial"
    - path: "src/pages/ResidentialPage.tsx"
      provides: "One residential CTA with ?type=residential"
      contains: "?type=residential"
    - path: "src/components/FloatingCTA.tsx"
      provides: "Commercial CTA with ?type=commercial and corrected text"
      contains: "?type=commercial"
    - path: "src/components/GCResourcesSection.tsx"
      provides: "Two commercial CTA links with ?type=commercial"
      contains: "?type=commercial"
  key_links:
    - from: "src/pages/HomePage.tsx"
      to: "/contact?type=commercial"
      via: "Link to attribute"
      pattern: '/contact\?type=commercial'
    - from: "src/pages/ResidentialPage.tsx"
      to: "/contact?type=residential"
      via: "Link to attribute"
      pattern: '/contact\?type=residential'
    - from: "src/components/FloatingCTA.tsx"
      to: "/contact?type=commercial"
      via: "Link to attribute"
      pattern: '/contact\?type=commercial'
---

<objective>
Update all CTA links across every page and component to append the correct `?type=` parameter and ensure consistent button text.

Purpose: CTA-01 requires all commercial CTAs to read "Request Prequal Package" and link with `?type=commercial`. CTA-02 requires the residential CTA to link with `?type=residential`. This plan sweeps every CTA identified in the research inventory and updates the link URLs and button text where needed.

Output: All 8 commercial CTAs link to `/contact?type=commercial` with correct text, and the 1 residential CTA links to `/contact?type=residential`. The FloatingCTA and AboutPage bottom CTA button text are corrected to match the required wording.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cta-simplification/05-RESEARCH.md
@.planning/phases/05-cta-simplification/05-01-SUMMARY.md

<interfaces>
<!-- CTA inventory from research. Each entry shows file, line, current text, and required change. -->

Commercial CTAs (9 link updates, 2 text changes):
1. HomePage.tsx ~line 277: Link to="/contact" -> to="/contact?type=commercial" (text "Request Prequal Package" already correct)
2. HomePage.tsx ~line 493: Link to="/contact" -> to="/contact?type=commercial" (text "Request Prequal Package" already correct)
3. FloatingCTA.tsx ~line 58: Link to="/contact" -> to="/contact?type=commercial" AND text "Download Prequal Package" -> "Request Prequal Package", mobile "Get Package" -> "Get Prequal"
4. GCResourcesSection.tsx ~line 91: Link to="/contact" -> to="/contact?type=commercial" (text "Request COI" kept as-is per research recommendation)
5. GCResourcesSection.tsx ~line 195: Link to="/contact" -> to="/contact?type=commercial" (text "Get in Touch" kept as-is per research recommendation)
6. AboutPage.tsx ~line 355: Link to="/contact" -> to="/contact?type=commercial" (text "Work With Us" kept as-is per research recommendation)
7. AboutPage.tsx ~line 376: Link to="/contact" -> to="/contact?type=commercial" AND text "Get a Free Consultation" -> "Request Prequal Package"
8. CaseStudyPage.tsx ~line 525: Link to="/contact" -> to="/contact?type=commercial" (text "Request Prequal Package" already correct)

Residential CTAs (1 link update, 0 text changes):
9. ResidentialPage.tsx ~line 216: Link to="/contact" -> to="/contact?type=residential" (text "Request a Quote" already correct)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update commercial CTA links and text across all pages and components</name>
  <files>src/pages/HomePage.tsx, src/pages/AboutPage.tsx, src/pages/CaseStudyPage.tsx, src/components/FloatingCTA.tsx, src/components/GCResourcesSection.tsx</files>
  <action>
    Update each file to append `?type=commercial` to all CTA links pointing to `/contact`. Also fix button text where it diverges from the CTA-01 spec.

    **HomePage.tsx** (2 links, no text changes):
    - Hero CTA (~line 277): Change `<Link to="/contact">` to `<Link to="/contact?type=commercial">`
    - Bottom CTA (~line 493): Change `<Link to="/contact">` to `<Link to="/contact?type=commercial">`

    **FloatingCTA.tsx** (1 link, text change):
    - Line 58: Change `<Link to="/contact">` to `<Link to="/contact?type=commercial">`
    - Change desktop text from "Download Prequal Package" to "Request Prequal Package" (the `<span className="hidden sm:inline">` element)
    - Change mobile text from "Get Package" to "Get Prequal" (the `<span className="sm:hidden">` element)
    - Change the icon from `Download` to `ArrowRight` in both the button and the decorative circle. Update the import: remove `Download` (if no longer used), add `ArrowRight`. The decorative circle icon (~line 44) and the button icon (~line 64) both change from `<Download>` to `<ArrowRight>`.

    **GCResourcesSection.tsx** (2 links, no text changes):
    - InfoCard "Request COI" link (~line 91): Change `to="/contact"` to `to="/contact?type=commercial"`. Keep "Request COI" text as-is (contextually accurate sub-CTA).
    - "Need More Details?" link (~line 195): Change `to="/contact"` to `to="/contact?type=commercial"`. Keep "Get in Touch" text as-is.

    **AboutPage.tsx** (2 links, 1 text change):
    - "Work With Us" link (~line 355): Change `<Link to="/contact">` to `<Link to="/contact?type=commercial">`. Keep "Work With Us" text as-is (softer mid-page tone per research).
    - Bottom CTA (~line 376): Change `<Link to="/contact">` to `<Link to="/contact?type=commercial">` AND change button text from "Get a Free Consultation" to "Request Prequal Package".

    **CaseStudyPage.tsx** (1 link, no text change):
    - Bottom CTA (~line 525): Change `<Link to="/contact">` to `<Link to="/contact?type=commercial">`. Text "Request Prequal Package" already correct.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearSite && npm run build</automated>
  </verify>
  <done>All 8 commercial CTA links include `?type=commercial`. FloatingCTA text reads "Request Prequal Package" (desktop) / "Get Prequal" (mobile) with ArrowRight icon. AboutPage bottom CTA reads "Request Prequal Package". Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update residential CTA link on ResidentialPage</name>
  <files>src/pages/ResidentialPage.tsx</files>
  <action>
    **ResidentialPage.tsx** (1 link, no text change):
    - Bottom CTA (~line 216): Change `<Link to="/contact">` to `<Link to="/contact?type=residential">`.
    - Button text "Request a Quote" is already correct per CTA-02 — do not change.

    After this change, run a comprehensive grep to confirm no CTA links to `/contact` remain without a `?type=` parameter (excluding navbar and footer navigation links which are intentionally plain):
    ```bash
    grep -rn 'to="/contact"' src/ --include="*.tsx"
    ```
    Expected results: only Navbar.tsx and Footer.tsx should have plain `/contact` links (these are navigation, not CTAs).
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearSite && npm run build && npm run lint</automated>
  </verify>
  <done>ResidentialPage CTA links to `/contact?type=residential`. Grep confirms no CTA links remain without `?type=` parameter (only Navbar/Footer navigation links are plain `/contact`).</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no TypeScript errors
2. `npm run lint` passes with no ESLint warnings
3. Grep: `grep -rn '?type=commercial' src/ --include="*.tsx"` shows 8 matches across HomePage, AboutPage, CaseStudyPage, FloatingCTA, GCResourcesSection
4. Grep: `grep -rn '?type=residential' src/ --include="*.tsx"` shows 1 match in ResidentialPage
5. Grep: `grep -rn 'to="/contact"' src/ --include="*.tsx"` shows ONLY Navbar.tsx and Footer.tsx (navigation links, not CTAs)
6. Grep: `grep -n 'Download Prequal Package' src/` returns no matches (FloatingCTA text replaced)
7. Grep: `grep -n 'Get a Free Consultation' src/` returns no matches (AboutPage text replaced)
</verification>

<success_criteria>
- All 8 commercial CTA links include `?type=commercial`
- 1 residential CTA link includes `?type=residential`
- FloatingCTA reads "Request Prequal Package" with ArrowRight icon
- AboutPage bottom CTA reads "Request Prequal Package"
- No plain `/contact` CTA links remain (only navigation links in Navbar/Footer)
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-cta-simplification/05-02-SUMMARY.md`
</output>
