---
phase: 04-prevailing-wage-service-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PrevailingWageBanner.tsx
  - src/pages/HomePage.tsx
  - src/pages/AboutPage.tsx
  - src/pages/ContactPage.tsx
autonomous: true
requirements: [WAGE-01, WAGE-02]

must_haves:
  truths:
    - "A GC visiting the HomePage sees a prevailing wage section naming PLA, Davis-Bacon, LAUSD, and State Prevailing Wage"
    - "A GC visiting the About page sees the same prevailing wage section after the SafetySection"
    - "The prevailing wage FAQ no longer appears on the Contact page"
    - "The Residential, Projects, and 404 pages do NOT show the prevailing wage section"
  artifacts:
    - path: "src/components/PrevailingWageBanner.tsx"
      provides: "Prevailing wage experience section component"
      exports: ["PrevailingWageBanner"]
    - path: "src/pages/HomePage.tsx"
      provides: "PrevailingWageBanner rendered between CertificationsBadges and GCPainPoints"
      contains: "PrevailingWageBanner"
    - path: "src/pages/AboutPage.tsx"
      provides: "PrevailingWageBanner rendered after SafetySection"
      contains: "PrevailingWageBanner"
    - path: "src/pages/ContactPage.tsx"
      provides: "FAQ array without prevailing wage entry"
  key_links:
    - from: "src/components/PrevailingWageBanner.tsx"
      to: "src/data/credentials.ts"
      via: "import DIR_STATUS and PREVAILING_WAGE_STATUS"
      pattern: "import.*credentials"
    - from: "src/pages/HomePage.tsx"
      to: "src/components/PrevailingWageBanner.tsx"
      via: "import and render in JSX"
      pattern: "<PrevailingWageBanner"
    - from: "src/pages/AboutPage.tsx"
      to: "src/components/PrevailingWageBanner.tsx"
      via: "import and render in JSX"
      pattern: "<PrevailingWageBanner"
---

<objective>
Build the PrevailingWageBanner component and wire it into HomePage and AboutPage, then remove the prevailing wage FAQ entry from ContactPage.

Purpose: Surface CGI's named prevailing wage experience (PLA, Davis-Bacon, LAUSD, State PW) where GCs evaluate subcontractors -- on the HomePage and About page -- rather than buried in a Contact page FAQ that visitors rarely reach.

Output: New PrevailingWageBanner.tsx component, updated HomePage.tsx and AboutPage.tsx with the banner rendered inline, updated ContactPage.tsx with the prevailing wage FAQ entry removed.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-prevailing-wage-service-depth/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/data/credentials.ts:
```typescript
export const DIR_STATUS = 'DIR Registered' as const;
export const PREVAILING_WAGE_STATUS = 'Prevailing Wage Certified' as const;
```

From src/components/SectionHeader.tsx (reusable heading component):
```typescript
export function SectionHeader({
  subheading, title, description?, subheadingColor?, titleSize?, className?
}): React.JSX.Element
```

From src/pages/HomePage.tsx — section order around insertion point (lines 339-343):
```tsx
<CertificationsBadges />
{/* INSERT PrevailingWageBanner HERE */}
<GCPainPoints />
```

From src/pages/AboutPage.tsx — section order around insertion point (lines 227-231):
```tsx
<SafetySection />
{/* INSERT PrevailingWageBanner HERE */}
{/* Mission, Goal, Promise */}
<section id="vision" className="py-20 bg-white">
```

From src/pages/ContactPage.tsx — FAQ entry to remove (lines 67-70):
```tsx
{
  question: 'Do you do prevailing wage work?',
  answer: 'Yes. CGI is DIR registered and fully set up for prevailing wage projects...',
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PrevailingWageBanner component and wire into HomePage and AboutPage</name>
  <files>src/components/PrevailingWageBanner.tsx, src/pages/HomePage.tsx, src/pages/AboutPage.tsx</files>
  <action>
Create `src/components/PrevailingWageBanner.tsx` as a named export (no default export). The component renders an inline section (NOT fixed/sticky -- no z-index needed) displaying CGI's prevailing wage experience types.

**Component structure:**
- Import `SectionHeader` from `./SectionHeader`
- Import `DIR_STATUS` and `PREVAILING_WAGE_STATUS` from `../data/credentials`
- Import icons from `lucide-react`: `FileCheck`, `CheckCircle2`, `Scale`, `GraduationCap` (or similar -- pick 4 distinct icons for the 4 wage types)
- Define a local `prevailingWageTypes` array with 4 entries:
  1. PLA (Project Labor Agreement) -- hiring hall obligations, union coordination
  2. Davis-Bacon Act -- federal prevailing wage for public works
  3. State Prevailing Wage (CA) -- DIR registered, certified payroll capability
  4. LAUSD and Institutional -- public-owner projects track record
- Each entry has: `label`, `description`, `icon`
- Render with `id="prevailing-wage"`, using `SectionHeader` for the heading
- Subheading: "Prevailing Wage Experience" (or "Compliance & Experience")
- Title: "Certified for Public Works Projects" (or similar -- keep professional, not promotional)
- Layout: 2x2 grid of cards on desktop (`grid-cols-2`), stacked on mobile (`grid-cols-1`)
- Each card shows icon + label (bold) + short description
- Card styling: consistent with the project's credential card patterns (rounded borders, subtle shadows)

**Background colors per page context:**
- The component should accept an optional `className` prop for outer section styling
- On HomePage: inserted between `<CertificationsBadges />` (bg-white) and `<GCPainPoints />` (bg-white). Use `bg-slate-50` to provide visual separation.
- On AboutPage: inserted between `<SafetySection />` (bg-slate-50) and the Mission/Goal/Promise section (bg-white). Use `bg-white` to alternate from SafetySection.

**Wire into HomePage.tsx:**
- Add import: `import { PrevailingWageBanner } from '../components/PrevailingWageBanner';`
- Insert `<PrevailingWageBanner className="bg-slate-50" />` between `<CertificationsBadges />` and `<GCPainPoints />` (after line 340, before line 343)

**Wire into AboutPage.tsx:**
- Add import: `import { PrevailingWageBanner } from '../components/PrevailingWageBanner';`
- Insert `<PrevailingWageBanner className="bg-white" />` after `<SafetySection />` (after line 228) and before the Mission/Goal/Promise section

**Do NOT:**
- Put PrevailingWageBanner in App.tsx (would render on excluded routes)
- Use fixed/sticky positioning (z-index collision with FloatingCTA at z-40)
- Use default export (project convention is named exports only)
- Use arbitrary hex colors (use Tailwind brand/accent tokens)
- Add any testimonial content (content rules prohibit fabricated quotes)
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearSite && npm run build && npm run lint</automated>
  </verify>
  <done>
- PrevailingWageBanner.tsx exists with named export
- All 4 wage types (PLA, Davis-Bacon, State PW, LAUSD) are named in the component
- HomePage renders the banner between CertificationsBadges and GCPainPoints with bg-slate-50
- AboutPage renders the banner after SafetySection with bg-white
- Build and lint pass with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove prevailing wage FAQ from ContactPage</name>
  <files>src/pages/ContactPage.tsx</files>
  <action>
In `src/pages/ContactPage.tsx`, locate the `faqs` array and remove the entry with question "Do you do prevailing wage work?" (currently at index 3 in the array, around lines 67-70).

The remaining 4 FAQ entries should be:
1. "What's your typical response time?" (turnaround)
2. "What areas do you serve?" (geography)
3. "What size projects do you handle?" (project scale / bonding)
4. "How do I get a bid?" (process)

Do NOT modify any other part of ContactPage.tsx. The prevailing wage content does not need to be "migrated" -- PrevailingWageBanner (created in Task 1) covers this content with more detail in a better location.

**Verify the removal is clean:** After removing the FAQ entry, confirm no orphaned commas, no empty array slots, and the remaining 4 entries render correctly.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearSite && npm run build && npm run lint && grep -c "prevailing wage" src/pages/ContactPage.tsx</automated>
  </verify>
  <done>
- The faqs array in ContactPage.tsx has exactly 4 entries (not 5)
- No entry mentions "prevailing wage" in the faqs array
- The grep count for "prevailing wage" in ContactPage.tsx is 0 or only appears in a badge/credential context (not in the faqs array)
- Build and lint pass
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes with no errors
- `npm run lint` completes with no warnings/errors
- `grep -rn "PrevailingWageBanner" src/pages/HomePage.tsx src/pages/AboutPage.tsx` shows imports and usage on both pages
- `grep -rn "PrevailingWageBanner" src/pages/ResidentialPage.tsx src/pages/ProjectsPage.tsx src/App.tsx` returns no matches (banner is NOT on excluded pages)
- `grep -n "prevailing wage" src/pages/ContactPage.tsx` shows 0 matches in the faqs array section
</verification>

<success_criteria>
- PrevailingWageBanner component exists and is rendered on HomePage (bg-slate-50) and AboutPage (bg-white)
- All 4 named wage types visible: PLA, Davis-Bacon, LAUSD, State Prevailing Wage
- PrevailingWageBanner does NOT appear on Residential, Projects, or 404 pages
- Contact page FAQ has 4 entries with no prevailing wage question
- Build and lint green
</success_criteria>

<output>
After completion, create `.planning/phases/04-prevailing-wage-service-depth/04-01-SUMMARY.md`
</output>
