---
phase: 06-typography-layout-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/pages/HomePage.tsx
  - src/pages/AboutPage.tsx
  - src/pages/ProjectsPage.tsx
  - src/pages/ResidentialPage.tsx
  - src/pages/ContactPage.tsx
  - src/pages/CaseStudyPage.tsx
  - src/pages/NotFoundPage.tsx
  - src/components/ProcessTimeline.tsx
  - src/components/GCPainPoints.tsx
  - src/components/CertificationsBadges.tsx
  - src/components/SafetySection.tsx
  - src/components/PrevailingWageBanner.tsx
  - src/components/TestimonialCarousel.tsx
  - src/components/PromiseSection.tsx
  - src/components/ServiceAreaMap.tsx
  - src/components/GCResourcesSection.tsx
  - src/components/Footer.tsx
  - src/components/ContactForm.tsx
  - src/components/ErrorBoundary.tsx
autonomous: false
requirements:
  - DSGN-01
  - DSGN-02

must_haves:
  truths:
    - "Every SectionHeader call site specifies a variant prop (no defaults used in production)"
    - "Section headers across HomePage use at least 3 different visual layouts"
    - "All h1-h6 headings render in Bebas Neue with uppercase treatment"
    - "Stat numbers and badges use font-heading but keep their contextual colors"
    - "Footer headings use font-heading uppercase but keep text-white"
    - "Body text, buttons, nav links, form labels are NOT affected by font changes"
    - "The site no longer looks like every section was stamped from the same SectionHeader template"
  artifacts:
    - path: "src/pages/HomePage.tsx"
      provides: "Multiple SectionHeader variants demonstrating layout variety"
      contains: "variant="
    - path: "src/pages/AboutPage.tsx"
      provides: "SectionHeader variants for story, milestones, team, CTA sections"
      contains: "variant="
    - path: "src/components/Footer.tsx"
      provides: "Footer headings with industrial font"
      contains: "font-heading"
  key_links:
    - from: "src/pages/HomePage.tsx"
      to: "src/components/SectionHeader.tsx"
      via: "SectionHeader import with variant prop"
      pattern: "variant=.(left-bar|banner|overlapping)"
    - from: "src/components/ProcessTimeline.tsx"
      to: "src/components/SectionHeader.tsx"
      via: "SectionHeader import with variant prop"
      pattern: "variant="
---

<objective>
Apply industrial heading font and SectionHeader variants across all pages and components site-wide.

Purpose: Complete the visual transformation by assigning layout variants to all ~19 SectionHeader consumers and sweeping all standalone h1-h6 elements with font-heading uppercase tracking-wide text-brand classes. This is the bulk application of the foundation built in Plan 01.
Output: Every heading on every page renders in Bebas Neue uppercase; every section header uses one of three distinct layout variants.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-typography-layout-foundation/06-CONTEXT.md
@.planning/phases/06-typography-layout-foundation/06-RESEARCH.md
@.planning/phases/06-typography-layout-foundation/06-01-SUMMARY.md

<interfaces>
<!-- SectionHeader interface after Plan 01 refactor -->
From src/components/SectionHeader.tsx (after Plan 01):
```typescript
type SectionHeaderVariant = 'left-bar' | 'banner' | 'overlapping';

interface SectionHeaderProps {
  subheading: string;
  title: string;
  description?: React.ReactNode;
  subheadingColor?: string;
  titleSize?: 'sm' | 'lg';
  className?: string;
  variant?: SectionHeaderVariant; // defaults to 'left-bar'
}
```

<!-- Tailwind utility available after Plan 01 -->
Tailwind class `font-heading` maps to Bebas Neue font family.
Standard heading pattern: `font-heading uppercase tracking-wide text-brand`
Exception for white backgrounds: keep `text-brand`
Exception for dark backgrounds: use `text-white` instead of `text-brand`
Exception for stat numbers: use `font-heading` but keep contextual color (not text-brand)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assign SectionHeader variants to all consumers and sweep standalone headings</name>
  <files>
    src/pages/HomePage.tsx,
    src/pages/AboutPage.tsx,
    src/pages/ProjectsPage.tsx,
    src/pages/ResidentialPage.tsx,
    src/pages/ContactPage.tsx,
    src/pages/CaseStudyPage.tsx,
    src/pages/NotFoundPage.tsx,
    src/components/ProcessTimeline.tsx,
    src/components/GCPainPoints.tsx,
    src/components/CertificationsBadges.tsx,
    src/components/SafetySection.tsx,
    src/components/PrevailingWageBanner.tsx,
    src/components/TestimonialCarousel.tsx,
    src/components/PromiseSection.tsx,
    src/components/ServiceAreaMap.tsx,
    src/components/GCResourcesSection.tsx,
    src/components/Footer.tsx,
    src/components/ContactForm.tsx,
    src/components/ErrorBoundary.tsx
  </files>
  <action>
This is a two-part sweep across all pages and components.

**PART A: Add variant prop to every SectionHeader call site (19 instances)**

Use this variant assignment (from research, optimized for visual rhythm -- no two adjacent sections share the same variant):

| File | Section Context | Variant |
|------|----------------|---------|
| HomePage.tsx | Services section | `left-bar` |
| AboutPage.tsx | Story section | `left-bar` |
| AboutPage.tsx | Milestones | `banner` |
| AboutPage.tsx | Team section | `overlapping` |
| AboutPage.tsx | CTA section | `left-bar` |
| ResidentialPage.tsx | Services | `left-bar` |
| ResidentialPage.tsx | Process | `banner` |
| ContactPage.tsx | Contact section | `banner` |
| CaseStudyPage.tsx | 5 sections | Alternate: `left-bar`, `banner`, `overlapping`, `left-bar`, `banner` |
| ProcessTimeline.tsx | Process header | `left-bar` |
| GCPainPoints.tsx | Pain points header | `overlapping` |
| CertificationsBadges.tsx | Certs header | `banner` |
| SafetySection.tsx | Safety header | `left-bar` |
| PrevailingWageBanner.tsx | Wage header | `banner` |
| TestimonialCarousel.tsx | Testimonials header | `left-bar` |

Add `variant="..."` prop to each `<SectionHeader` call. Example:
```tsx
// Before
<SectionHeader subheading="..." title="..." />
// After
<SectionHeader variant="left-bar" subheading="..." title="..." />
```

**PART B: Apply font-heading to ALL standalone headings (not inside SectionHeader)**

Find every `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>` element across ALL files and apply the industrial font treatment:

Standard heading pattern -- add these classes:
- `font-heading` (Bebas Neue font)
- `uppercase` (all-caps per user decision)
- `tracking-wide` (letter spacing for industrial feel)
- Replace `text-slate-900` with `text-brand` wherever it appears on headings

**Color exceptions (do NOT use text-brand on these):**
- Headings on dark backgrounds: use `text-white` (e.g., Footer h3 elements, any heading inside a dark section)
- Stat numbers/counters: apply `font-heading` but keep their existing contextual colors (these are numbers, not text headings)

**Elements to NOT change (per CONTEXT.md):**
- Body paragraph text (`<p>` elements)
- Button text
- Navigation link text (Navbar links)
- Form labels and inputs
- Small metadata text (dates, categories, badge text)

**Specific file guidance:**
- `Footer.tsx`: h3 elements ("Quick Links", "Services", "Contact") get `font-heading uppercase` but keep `text-white`
- `HomePage.tsx`: Hero h1, any inline h2/h3 section titles get full treatment
- `ProjectsPage.tsx`: Page h1 and any card headings get `font-heading uppercase tracking-wide text-brand`
- `NotFoundPage.tsx`: 404 heading gets `font-heading uppercase tracking-wide text-brand`
- `ErrorBoundary.tsx`: Error heading gets `font-heading uppercase tracking-wide text-brand`
- `ContactForm.tsx`: Form heading (if h-tag exists) gets treatment; form labels do NOT
- `ProcessTimeline.tsx`: Step titles (h3/h4 within steps) get `font-heading uppercase` but may keep smaller sizing
- `GCPainPoints.tsx`: Pain point card titles get `font-heading uppercase`
- `PromiseSection.tsx`: Promise headings get full treatment
- `ServiceAreaMap.tsx`: Section heading gets full treatment
- `GCResourcesSection.tsx`: Section heading gets full treatment

**After all changes, verify completeness:**
Run `grep -rn '<h[1-6]' src/ | grep -v 'font-heading'` to find any missed headings. Every h1-h6 should have `font-heading` class (the only exceptions should be the ghost title in SectionHeader's overlapping variant which already has it, and the aria-hidden duplicate).
  </action>
  <verify>
    <automated>cd /c/Users/relmq/projects/clearsite && npm run build 2>&1 | tail -5 && echo "---VARIANT CHECK---" && grep -rn 'variant=' src/pages/ src/components/ProcessTimeline.tsx src/components/GCPainPoints.tsx src/components/CertificationsBadges.tsx src/components/SafetySection.tsx src/components/PrevailingWageBanner.tsx src/components/TestimonialCarousel.tsx | grep -c 'variant=' && echo "---HEADING CHECK---" && grep -rn '<h[1-6]' src/ | grep -v 'font-heading' | grep -v 'aria-hidden' | grep -v 'SectionHeader' | head -10</automated>
  </verify>
  <done>All 19 SectionHeader consumers specify a variant prop; all standalone h1-h6 elements across the site include font-heading uppercase tracking-wide; heading text color is text-brand (or text-white on dark backgrounds); build passes; no heading element is missing font-heading class</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of typography and layout variants</name>
  <action>Visual review of all pages to confirm industrial typography and varied section header layouts render correctly across all routes and viewport sizes.</action>
  <what-built>Industrial typography and varied section header layouts applied site-wide. Bebas Neue font on all headings, three distinct SectionHeader variants (left-bar with orange accent, full-width dark banner, overlapping ghost title) assigned across all pages.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173/
    2. Check HomePage:
       - Hero h1 renders in bold, condensed, all-caps Bebas Neue font (navy color)
       - Services SectionHeader has orange vertical bar on left, text left-aligned
       - Different sections use visibly different header layouts
    3. Check About page (/about):
       - Story section: left-bar variant (orange bar)
       - Milestones: banner variant (full-width dark navy background, white text)
       - Team: overlapping variant (large ghost title behind real title)
    4. Check Contact page (/contact):
       - Banner variant header (dark navy background)
    5. Check Projects page (/projects) and a case study:
       - All headings in Bebas Neue uppercase
       - Case study sections alternate between variants
    6. Check Residential page (/residential):
       - Services: left-bar, Process: banner
    7. Scroll through every page checking:
       - NO heading still shows the old system font
       - NO two adjacent sections use the same header layout
       - Body text, buttons, nav links, form labels are UNCHANGED
       - Footer headings are uppercase Bebas Neue in white
    8. Check mobile viewport (resize to ~375px):
       - No horizontal scrollbar (especially on banner variant pages)
       - All layouts remain readable
  </how-to-verify>
  <verify>Visual inspection by user</verify>
  <done>User confirms all headings render in Bebas Neue, all section headers show correct variant layouts, no visual regressions on any page or viewport</done>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `npm run lint` passes
- All 19 SectionHeader call sites have explicit `variant=` prop
- Zero `<h1-6>` elements missing `font-heading` class (excluding aria-hidden ghost)
- No horizontal scrollbar on any page at any viewport width
- Visual confirmation: headings are Bebas Neue uppercase throughout
</verification>

<success_criteria>
- Every section header on every page uses one of three distinct visual layouts (left-bar, banner, overlapping)
- HomePage alone demonstrates all three layout variants
- All headings site-wide render in Bebas Neue uppercase with navy or white color
- Body text, buttons, nav links, form labels are completely unaffected
- No build or lint errors
- No horizontal overflow from banner variant
</success_criteria>

<output>
After completion, create `.planning/phases/06-typography-layout-foundation/06-02-SUMMARY.md`
</output>
