---
phase: 07-service-copy-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/HomePage.tsx
  - src/components/ServiceCard.tsx
  - src/components/ProcessTimeline.tsx
autonomous: true
requirements:
  - COPY-01
  - COPY-02
  - DSGN-03
  - DSGN-04

must_haves:
  truths:
    - "Each of the 8 service descriptions leads with a GC benefit (schedule, coordination, callbacks) not a technical spec"
    - "Each service has 3 GC-outcome bullets instead of technical specification bullets"
    - "All 8 services use distinct lucide-react icons with no duplicates"
    - "ServiceCard hover gradient uses brand navy tint instead of generic blue"
    - "All 7 Process Timeline steps use GC-project terminology"
    - "Process Timeline step descriptions emphasize GC outcomes not glazing procedures"
  artifacts:
    - path: "src/pages/HomePage.tsx"
      provides: "Rewritten service data with GC-benefit copy and unique icons"
      contains: "from-brand"
    - path: "src/components/ServiceCard.tsx"
      provides: "Brand-consistent hover gradient"
      contains: "from-brand/5"
    - path: "src/components/ProcessTimeline.tsx"
      provides: "GC-terminology process steps"
      contains: "Budget Request"
  key_links:
    - from: "src/pages/HomePage.tsx"
      to: "src/components/ServiceCard.tsx"
      via: "services array passed as props"
      pattern: "icon.*description.*bullets"
    - from: "src/components/ProcessTimeline.tsx"
      to: "lucide-react"
      via: "icon imports for step icons"
      pattern: "import.*from 'lucide-react'"
---

<objective>
Rewrite all service and process content to speak to GCs about project outcomes, assign unique icons per service, and apply brand-consistent hover colors.

Purpose: Transform service content from technical glazing specs into GC-benefit language that emphasizes schedule reliability, coordination ease, and minimal callbacks — the things GC project managers actually care about.
Output: Updated service data in HomePage.tsx, brand hover in ServiceCard.tsx, GC-terminology process steps in ProcessTimeline.tsx
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-service-copy-refinement/07-CONTEXT.md
@src/pages/HomePage.tsx
@src/components/ServiceCard.tsx
@src/components/ProcessTimeline.tsx

<interfaces>
<!-- ServiceCard props interface — no changes to interface, only data values and hover styling -->
From src/components/ServiceCard.tsx:
```typescript
interface ServiceCardProps {
  title: string;
  description: string;
  icon: React.ReactNode;
  bullets?: string[];
}
```

<!-- Service data shape in HomePage.tsx (lines 45-133) — 8 objects in services array -->
Each service object: { title, description, icon: <LucideIcon className="h-6 w-6" />, bullets: string[] }

<!-- ProcessTimeline steps shape (lines 43-79) — 7 objects in steps array -->
Each step object: { icon: <LucideIcon className="h-5 w-5" />, title: string, description: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite service copy, assign unique icons, and fix hover color</name>
  <files>src/pages/HomePage.tsx, src/components/ServiceCard.tsx</files>
  <action>
**ServiceCard.tsx hover fix (DSGN-03):**
Line 24: Change `from-blue-50` to `from-brand/5` in the hover gradient background. Keep `to-transparent` unchanged. Per user decision, keep existing icon container hover (`bg-brand text-white shadow-brand/30`) and bottom accent line (`bg-gradient-to-r from-brand via-accent to-brand`). For the border on hover (currently `border-accent/30` on line 17), use Claude's judgment on whether to shift to `border-brand/30`.

**HomePage.tsx service data rewrite (COPY-01 + DSGN-04):**
Rewrite all 8 service objects in the `services` array (lines 45-133). For each service:

1. **Description** — Two-sentence pattern per user decision:
   - Sentence 1: GC benefit (schedule impact, coordination ease, minimal callbacks)
   - Sentence 2: What makes CGI different (single source, coordination, experience)
   - Use GC PM language: "on your timeline", "no schedule gaps", "minimal punch list", "first-pass inspections", "coordinates with other subs"
   - Distribute pain points naturally — no two cards should feel repetitive

2. **Bullets** — Replace all technical spec bullets with 3 GC-outcome bullets per service. Examples of good GC-outcome bullets: "Coordinates with framing and waterproofing trades", "Same-day budgets on plan review", "Minimal punch list items on final walkthrough". Do NOT use spec language like "thermally broken" or "UL-listed".

3. **Icons** — Assign unique lucide-react icons. Currently Maximize is used 3 times (Window Walls, Glass Railings, Mirrors). All must change. Style: literal/representational per user decision. Suggested mapping (use judgment):
   - Storefronts: `DoorOpen` (entrance/door)
   - Window Walls: `PanelTop` or `AppWindow` (window representation)
   - Curtain Walls: `Building2` (keep — fits commercial building)
   - Skylights: `Sun` (keep — represents overhead light)
   - Glass Railings: `Fence` or `Grip` (railing representation)
   - Fire-Rated Glazing: `Shield` (keep — represents safety/protection)
   - Shower Enclosures: `Bath` or `Droplets` (water/bathroom)
   - Mirrors: `RectangleHorizontal` or `Scan` (reflective surface)

   Update the lucide-react import block at top of file to include new icons and remove unused ones.

**Content rules to follow:**
- Use "same-day budgets" not "quotes" for commercial
- Use "minimal punch list" not "zero"
- Use "3-5 business days" for bid turnaround
- No fake testimonials or fabricated data
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npx tsc --noEmit 2>&1 | head -20 && node -e "const fs=require('fs'); const h=fs.readFileSync('src/pages/HomePage.tsx','utf8'); const icons=h.match(/<(\w+)\s+className=\"h-6 w-6\"/g); const unique=new Set(icons); console.log('Icons found:',icons.length,'Unique:',unique.size); if(icons.length!==unique.size) {console.log('FAIL: duplicate icons'); process.exit(1)} else {console.log('PASS: all icons unique')}; const s=fs.readFileSync('src/components/ServiceCard.tsx','utf8'); if(s.includes('from-blue-50')){console.log('FAIL: still has from-blue-50'); process.exit(1)} else {console.log('PASS: hover color updated')}"</automated>
  </verify>
  <done>All 8 services have GC-benefit descriptions (2 sentences each), 3 GC-outcome bullets each, unique lucide-react icons with zero duplicates, and ServiceCard hover uses brand/5 gradient instead of blue-50</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Process Timeline with GC terminology</name>
  <files>src/components/ProcessTimeline.tsx</files>
  <action>
Rewrite the `steps` array (lines 43-79) in ProcessTimeline.tsx. Per user decisions:

**Step names and descriptions — all 7 steps rewritten with GC-benefit language:**

1. **"Budget Request"** (locked name per user decision — NOT "Bid Request" or "Plan Review & Budget")
   - Description: Emphasize same-day budget turnaround, easy submission of plans and specs
   - Icon: Keep `FileText`

2. **"Site Assessment"** (per user direction)
   - Description: Emphasize field verification that prevents change orders, coordination with existing conditions
   - Icon: `MapPin` (keep)

3. **"Scope & Schedule"** (per user direction)
   - Description: Emphasize clear scope aligned to GC's master schedule, no surprises
   - Icon: `ClipboardList` (keep) or `CalendarCheck` if available

4. **"Submittals & Approvals"** (per user direction)
   - Description: Emphasize shop drawings submitted on the GC's timeline, architect coordination
   - Icon: `FileText` or `FileCheck` to differentiate from step 1

5. **"Fabrication"** (per user direction)
   - Description: Emphasize lead time tracking, proactive updates so GC can plan around delivery
   - Icon: `Wrench` (keep) or `Factory`

6. **"Installation"** (keep)
   - Description: Emphasize daily progress updates, coordination with other trades, clean jobsite
   - Icon: `Wrench` or `HardHat` to differentiate from Fabrication

7. **Last step — Claude's discretion** (user said blend of punchlist/turnover + warranty/docs is fine)
   - Suggested: "Turnover & Warranty" or "Closeout & Warranty"
   - Description: Emphasize minimal punch list, as-built docs, warranty package — everything the GC needs to close out
   - Icon: `CheckCircle` (keep)

Update the lucide-react import at top of file to include any new icons and remove unused ones.

**Content rules:** Use "minimal punch list" not "zero". Use "3-5 business days" not "48-72 hours".
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npx tsc --noEmit 2>&1 | head -20 && node -e "const fs=require('fs'); const p=fs.readFileSync('src/components/ProcessTimeline.tsx','utf8'); if(p.includes('Budget Request')){console.log('PASS: step 1 is Budget Request')} else {console.log('FAIL: step 1 not renamed'); process.exit(1)}; if(p.includes('Bid Request')){console.log('FAIL: old Bid Request still present'); process.exit(1)} else {console.log('PASS: no old Bid Request')}; const steps=p.match(/title:\s*'[^']+'/g); console.log('Steps found:',steps?.length); if(steps?.length!==7){console.log('FAIL: expected 7 steps'); process.exit(1)} else {console.log('PASS: 7 steps present')}"</automated>
  </verify>
  <done>All 7 Process Timeline steps use GC-project terminology with step 1 named "Budget Request", descriptions emphasize GC outcomes (schedule, coordination, minimal punch list), and no old technical/procedural language remains</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. All 8 service icons are unique lucide-react components (no duplicates)
3. ServiceCard hover gradient uses `from-brand/5` not `from-blue-50`
4. Service descriptions lead with GC benefits, not technical specs
5. Process Timeline step 1 is named "Budget Request"
6. No content rule violations ("quotes" used instead of "budgets", "zero" punch list, wrong turnaround time)
</verification>

<success_criteria>
- Service content reads like it was written for a GC project manager, not a glazing engineer
- Each service card has a visually distinct icon
- Hover states use brand colors consistently
- Process Timeline uses GC workflow terminology throughout
- `npm run build` passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/07-service-copy-refinement/07-01-SUMMARY.md`
</output>
