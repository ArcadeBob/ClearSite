---
phase: 11-page-structure-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/AboutPage.tsx
  - src/pages/ProjectsPage.tsx
  - src/pages/ContactPage.tsx
  - src/pages/ResidentialPage.tsx
autonomous: true
requirements:
  - PAGE-01
  - ABOUT-01

must_haves:
  truths:
    - "Inner page headers (About, Projects, Contact, Residential) display a richer visual treatment than plain navy banners"
    - "About page displays a glazing-relevant storefront/curtain wall photo instead of the current Citrus Commons project image"
  artifacts:
    - path: "src/pages/AboutPage.tsx"
      provides: "Upgraded header with gradient/pattern treatment and storefront image"
      contains: "bg-gradient"
    - path: "src/pages/ProjectsPage.tsx"
      provides: "Upgraded header with gradient/pattern treatment"
      contains: "bg-gradient"
    - path: "src/pages/ContactPage.tsx"
      provides: "Upgraded header with gradient/pattern treatment"
      contains: "bg-gradient"
    - path: "src/pages/ResidentialPage.tsx"
      provides: "Upgraded header with gradient/pattern treatment"
      contains: "bg-gradient"
  key_links: []
---

<objective>
Upgrade all inner page headers from plain flat navy banners to a richer visual treatment, and swap the About page Brand Story image to a glazing-relevant storefront detail photo.

Purpose: Plain bg-brand py-16 headers feel flat and generic. A richer treatment adds polish and visual hierarchy. The About page image should reflect the company's commercial glazing work.
Output: Four page files with upgraded headers, About page with new storefront image.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-page-structure-navigation/11-CONTEXT.md

<interfaces>
<!-- Current inner page header pattern (identical across all 4 pages): -->
```tsx
<div className="bg-brand text-white py-16">
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h1 className="font-heading uppercase tracking-wide text-4xl font-bold text-white mb-4">
      Page Title
    </h1>
    <p className="text-xl text-slate-300 max-w-3xl">
      Subtitle text
    </p>
  </div>
</div>
```

<!-- About page Brand Story image (AboutPage.tsx ~line 154-159): -->
```tsx
<img
  src="/images/projects/Citrus-Commons.jpg"
  alt="Citrus Commons commercial glazing project by Clean Glass Installation"
  className="relative rounded-xl shadow-xl w-full object-cover h-[450px]"
/>
```

<!-- Brand color tokens from tailwind.config.js: -->
<!-- brand (#1e3a5f), brand-dark (#162c47), accent (#2563eb), accent-dark (#1d4ed8) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade inner page headers to richer visual treatment</name>
  <files>src/pages/AboutPage.tsx, src/pages/ProjectsPage.tsx, src/pages/ContactPage.tsx, src/pages/ResidentialPage.tsx</files>
  <action>
Replace the plain `bg-brand text-white py-16` header in all four inner pages with a richer treatment that adds visual depth while keeping the brand navy identity. Apply the same pattern to all four pages for consistency.

Recommended treatment (Claude's discretion on exact styling):
- Use a subtle gradient: `bg-gradient-to-br from-brand-dark via-brand to-brand` for depth
- Add a faint geometric accent — a decorative diagonal stripe or subtle pattern overlay using a pseudo-element or a small decorative div (e.g., a thin accent-colored line or dot pattern at the bottom edge)
- Increase vertical padding slightly: `py-20` instead of `py-16` for more breathing room
- Add a subtle bottom border: `border-b-4 border-accent` or a gradient accent bar
- Keep the existing h1 + subtitle text structure, just enhance the container

The four page headers are at:
- AboutPage.tsx line ~103: "About Clean Glass Installation"
- ProjectsPage.tsx line ~260: "Project Portfolio"
- ContactPage.tsx line ~100: "Get In Touch"
- ResidentialPage.tsx line ~65: "Residential Services"

Do NOT change any content text, just the visual container styling. Keep font-heading on h1, text-slate-300 on subtitle.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All four inner page headers display a richer visual treatment with gradient backgrounds and accent details instead of flat plain navy banners. Text content unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Replace About page Brand Story image with storefront detail</name>
  <files>src/pages/AboutPage.tsx</files>
  <action>
Replace the Brand Story section image in AboutPage.tsx (~line 156-159) with a glazing-relevant Unsplash storefront detail photo.

Find a high-quality Unsplash image of a modern commercial storefront — glass panels, aluminum framing, clean lines. Use the Unsplash source URL format: `https://images.unsplash.com/photo-{ID}?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80`

Good search terms: modern commercial storefront glass facade, curtain wall detail, glass building entrance.

Update:
- `src` attribute to the new Unsplash URL
- `alt` attribute to describe the new image: "Modern commercial storefront with glass panels and aluminum framing"
- Keep the existing className and container styling (rounded-xl, shadow-xl, h-[450px], etc.)
- Keep the decorative amber background rotation div above it
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>About page Brand Story section displays a glazing-relevant storefront/curtain wall detail image (Unsplash placeholder) instead of the Citrus Commons project photo.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- All four inner pages render with gradient/enhanced headers (not flat navy)
- About page Brand Story section shows a storefront detail image
</verification>

<success_criteria>
- Inner page headers on About, Projects, Contact, and Residential pages display a visually richer treatment than the previous plain navy banners
- About page image shows a glazing-relevant storefront photo
- No text content changes, no layout structure changes
</success_criteria>

<output>
After completion, create `.planning/phases/11-page-structure-navigation/11-01-SUMMARY.md`
</output>
