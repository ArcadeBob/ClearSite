---
phase: 02-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/AboutPage.tsx
autonomous: false
requirements:
  - ABUT-01

must_haves:
  truths:
    - "The About page brand story section displays a glazing/construction crew photo instead of the previous generic stock image"
    - "The replacement image is landscape-oriented and fills the container without layout shifts"
    - "The image src uses a direct images.unsplash.com/photo-{ID} URL matching the codebase pattern"
    - "The alt text describes the new image content"
    - "The decorative amber-50 rotated card behind the image is unchanged"
  artifacts:
    - path: "src/pages/AboutPage.tsx"
      provides: "About page with domain-appropriate crew/glazing image"
      contains: "images.unsplash.com/photo-"
  key_links: []
---

<objective>
Replace the About page brand story stock image with a domain-appropriate commercial glazing/construction crew Unsplash photo.

Purpose: The current image (`photo-1581094794329-c8112a89af12`) is a generic stock photo unrelated to glazing. A construction crew or glass installation photo better represents CGI's work and builds credibility with GC visitors.

Output: Updated `AboutPage.tsx` with a verified landscape Unsplash photo showing commercial construction/glazing work.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-polish/02-RESEARCH.md
@src/pages/AboutPage.tsx

<interfaces>
<!-- Image container from AboutPage.tsx (lines 146-154) — only src and alt change -->

```tsx
<div className="relative">
  <div className="absolute -inset-4 bg-amber-50 rounded-2xl transform -rotate-2"></div>
  <img
    src="https://images.unsplash.com/photo-{ID}?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80"
    alt="{description of new image}"
    className="relative rounded-xl shadow-xl w-full object-cover h-[450px]"
  />
</div>
```

Container constraints:
- Fixed height: `h-[450px]` with `object-cover` — landscape orientation required
- Width: `w-full` — fills right column (~600px rendered)
- Decorative div behind image: UNCHANGED

Candidate Unsplash photo IDs (from research, need visual verification):
- `photo-1504307651254-35680f356dfd` — Construction workers on building exterior
- `photo-1541888946425-d81bb19240f5` — Construction crew at work site
- `photo-1565008447742-97f6f38c985c` — Workers installing glass panels / facade work
- `photo-1599707367072-cd6ada2bc375` — Commercial glazing / window installation crew

URL pattern: `https://images.unsplash.com/photo-{ID}?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preview candidate images and select best glazing/crew photo</name>
  <files>src/pages/AboutPage.tsx</files>
  <action>
Preview each candidate Unsplash photo ID by opening the URL in the browser (append `?w=200&q=80` for quick preview). Select the photo that best shows:
1. Workers handling glass panels, aluminum framing, or curtain wall/storefront installation
2. Landscape orientation (wider than tall)
3. Clear, professional construction site scene

Candidate URLs to preview:
- https://images.unsplash.com/photo-1504307651254-35680f356dfd?w=200&q=80
- https://images.unsplash.com/photo-1541888946425-d81bb19240f5?w=200&q=80
- https://images.unsplash.com/photo-1565008447742-97f6f38c985c?w=200&q=80
- https://images.unsplash.com/photo-1599707367072-cd6ada2bc375?w=200&q=80

If none of these load or are appropriate, search Unsplash for "commercial glass installation crew" or "curtain wall construction workers" and select a landscape photo with a direct photo-ID URL.

Once selected, update `src/pages/AboutPage.tsx`:
1. Find the img tag around line 149 with `src="https://images.unsplash.com/photo-1581094794329-c8112a89af12..."`
2. Replace the `src` attribute with the selected photo URL using the codebase pattern: `https://images.unsplash.com/photo-{SELECTED_ID}?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80`
3. Update the `alt` attribute to describe the new image (e.g., "Commercial glazing crew installing glass panels" or "Construction workers on a building site")

DO NOT change any other attributes on the img tag (className stays the same).
DO NOT change the decorative `absolute` div behind the image.
DO NOT change any other part of AboutPage.tsx.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearSite && npx tsc --noEmit 2>&1 | head -5 && grep -n "images.unsplash.com" src/pages/AboutPage.tsx | head -5</automated>
  </verify>
  <done>
- AboutPage.tsx img src updated to a new Unsplash photo ID (not `photo-1581094794329-c8112a89af12`)
- Alt text updated to reflect new image subject
- Image container markup (className, wrapper divs) unchanged
- TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of About page image</name>
  <files>src/pages/AboutPage.tsx</files>
  <action>
What was built: Replaced the About page brand story image with a commercial glazing/construction crew Unsplash photo. The image container, decorative styling, and all other page content are unchanged.

How to verify:
1. Run `npm run dev` and open http://localhost:5173/about
2. Scroll to the "Our Story" / brand story section (right column image)
3. Confirm the image shows a construction crew, glass installation, or similar glazing-relevant scene
4. Confirm the image fills the container without layout shifts, black bars, or heavy cropping
5. Confirm the decorative amber-50 rotated card behind the image is still visible
6. Compare with /projects and / (homepage) to ensure the overall visual quality is consistent

Resume signal: Type "approved" if the image looks good, or describe what's wrong (e.g., "too dark", "not relevant to glazing", "cropped badly").
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearSite && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>
- User has visually approved the replacement image on the About page
- Image shows a glazing/construction crew scene relevant to CGI's work
- Image fills the container without layout issues
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `grep 'photo-1581094794329-c8112a89af12' src/pages/AboutPage.tsx` returns NO matches (old image removed)
3. `grep 'images.unsplash.com/photo-' src/pages/AboutPage.tsx` returns a match with the new photo ID
4. The image visually shows a construction/glazing crew scene (human verified)
5. No layout shifts in the image container (human verified)
</verification>

<success_criteria>
- ABUT-01 complete: About page brand story section displays a glazing/construction crew Unsplash photo
- Image is landscape-oriented, fills the h-[450px] container cleanly with object-cover
- Alt text is descriptive and relevant to the new image
- No other changes to AboutPage.tsx
- Human has visually approved the image choice
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-polish/02-02-SUMMARY.md`
</output>
