---
phase: 02-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ContactPage.tsx
autonomous: true
requirements:
  - CONT-01

must_haves:
  truths:
    - "The Contact page hero uses an <h1> element (not <h2>) for the page title"
    - "The Contact page hero title text is visible (white on dark navy background)"
    - "The Contact page hero layout matches the About and Projects page heroes — same padding, container width, and typography classes"
    - "The Contact page hero has no uppercase subheading label above the title"
    - "The Contact page title text ('Request Prequalification Packet') and description text are unchanged"
    - "The SectionHeader component still works in the FAQ section further down the page"
  artifacts:
    - path: "src/pages/ContactPage.tsx"
      provides: "Contact page with restyled hero matching About/Projects pattern"
      contains: "<h1 className=\"text-4xl font-bold mb-4\">"
  key_links:
    - from: "src/pages/ContactPage.tsx"
      to: "SectionHeader"
      via: "import statement"
      pattern: "import.*SectionHeader"
---

<objective>
Restyle the Contact page hero section to match the visual pattern used on the About and Projects pages.

Purpose: The Contact page hero currently uses `SectionHeader` which renders an `<h2>` with hardcoded `text-slate-900` (invisible on the dark `bg-brand` background). The About and Projects pages use raw `<h1>` + `<p>` markup that is correct for dark-background heroes.

Output: Updated `ContactPage.tsx` with hero markup matching the About/Projects pattern.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-polish/02-RESEARCH.md
@src/pages/ContactPage.tsx

<interfaces>
<!-- The hero pattern from About/Projects pages that Contact must match -->

Target hero markup pattern (from AboutPage.tsx and ProjectsPage.tsx):
```tsx
<div className="bg-brand text-white py-16">
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h1 className="text-4xl font-bold mb-4">
      {/* page title */}
    </h1>
    <p className="text-xl text-slate-300 max-w-3xl">
      {/* subtitle */}
    </p>
  </div>
</div>
```

Current Contact hero (REPLACE THIS):
```tsx
<div className="bg-brand text-white py-16">
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <SectionHeader
      subheading="For General Contractors"
      title="Request Prequalification Packet"
      description="Get everything you need to add CGI to your bid list — COI, EMR, references, and project history — delivered within 24 hours."
      subheadingColor="text-amber-300"
      className="text-left"
    />
  </div>
</div>
```

IMPORTANT: The `SectionHeader` import MUST remain because it is used again in the FAQ section (~line 245).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Contact hero SectionHeader with raw h1+p markup</name>
  <files>src/pages/ContactPage.tsx</files>
  <action>
In `src/pages/ContactPage.tsx`, find the hero section (around lines 74-84) containing the `SectionHeader` call inside the `bg-brand` div.

Replace this block:
```tsx
    <SectionHeader
      subheading="For General Contractors"
      title="Request Prequalification Packet"
      description="Get everything you need to add CGI to your bid list — COI, EMR, references, and project history — delivered within 24 hours."
      subheadingColor="text-amber-300"
      className="text-left"
    />
```

With this raw markup (matching the About/Projects hero pattern):
```tsx
    <h1 className="text-4xl font-bold mb-4">
      Request Prequalification Packet
    </h1>
    <p className="text-xl text-slate-300 max-w-3xl">
      Get everything you need to add CGI to your bid list — COI, EMR,
      references, and project history — delivered within 24 hours.
    </p>
```

DO NOT remove the `SectionHeader` import — it is still used in the FAQ section further down the page. Verify with grep that `SectionHeader` appears at least once more in the file after the hero edit.

DO NOT change the outer `<div className="bg-brand text-white py-16">` wrapper or the inner `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">` container.

DO NOT change the title text or description text — this is a style-only update (per STATE.md decision).
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearSite && npx tsc --noEmit 2>&1 | head -20 && grep -n "SectionHeader" src/pages/ContactPage.tsx && grep -n "<h1" src/pages/ContactPage.tsx</automated>
  </verify>
  <done>
- ContactPage.tsx hero renders `<h1>` with `text-4xl font-bold mb-4` (not SectionHeader `<h2>`)
- Title text "Request Prequalification Packet" preserved exactly
- Description text preserved exactly
- SectionHeader import retained (still used in FAQ section)
- TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build and hero pattern consistency</name>
  <files>src/pages/ContactPage.tsx</files>
  <action>
Run the production build to confirm zero errors. Then verify the hero pattern consistency across all three pages (About, Projects, Contact) by checking they all use the same `<h1 className="text-4xl font-bold mb-4">` pattern inside `<div className="bg-brand text-white py-16">`.

Verification checks:
1. `npm run build` succeeds with no errors
2. All three pages (AboutPage, ProjectsPage, ContactPage) have `<h1 className="text-4xl font-bold mb-4">` inside a `bg-brand` hero section
3. ContactPage no longer has `SectionHeader` in the hero block (but still has it elsewhere)
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearSite && npm run build 2>&1 | tail -5 && echo "--- Hero h1 pattern across pages ---" && grep -n 'text-4xl font-bold mb-4' src/pages/AboutPage.tsx src/pages/ProjectsPage.tsx src/pages/ContactPage.tsx</automated>
  </verify>
  <done>
- `npm run build` exits 0 with no errors
- All three hero pages (About, Projects, Contact) share the same `<h1>` class pattern
- CONT-01 requirement satisfied: Contact hero visually matches About and Projects
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `grep '<h1' src/pages/ContactPage.tsx` shows an h1 tag in the hero section
3. `grep 'SectionHeader' src/pages/ContactPage.tsx` still returns at least one match (FAQ section usage)
4. The hero `<h1>` classes match About/Projects: `text-4xl font-bold mb-4`
5. The hero `<p>` classes match About/Projects: `text-xl text-slate-300 max-w-3xl`
</verification>

<success_criteria>
- CONT-01 complete: Contact page hero uses the same raw `<h1>` + `<p>` markup pattern as About and Projects pages
- Title and description text are unchanged (style-only update)
- SectionHeader still functional in the FAQ section
- Production build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-polish/02-01-SUMMARY.md`
</output>
