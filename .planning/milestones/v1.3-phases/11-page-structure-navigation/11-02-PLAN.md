---
phase: 11-page-structure-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/components/ProjectCard.tsx
  - src/pages/ProjectsPage.tsx
  - src/components/Footer.tsx
autonomous: true
requirements:
  - PAGE-02
  - PAGE-03

must_haves:
  truths:
    - "Clicking a project card body navigates to its case study page (for projects with case studies)"
    - "Clicking a project card body flips the card (for projects without case studies)"
    - "A dedicated Quick Stats button on the card front triggers the flip animation"
    - "Footer displays 3 columns (Company, Services, Contact & Links) instead of 4"
  artifacts:
    - path: "src/components/ProjectCard.tsx"
      provides: "Refactored card with separate click targets for navigation vs flip"
      contains: "Quick Stats"
    - path: "src/pages/ProjectsPage.tsx"
      provides: "Passes caseStudySlug prop to ProjectCard"
      contains: "caseStudySlug"
    - path: "src/components/Footer.tsx"
      provides: "3-column footer layout"
      contains: "md:grid-cols-3"
  key_links:
    - from: "src/pages/ProjectsPage.tsx"
      to: "src/components/ProjectCard.tsx"
      via: "caseStudySlug prop"
      pattern: "caseStudySlug.*cabrillo"
    - from: "src/components/ProjectCard.tsx"
      to: "/projects/:slug"
      via: "useNavigate or Link"
      pattern: "navigate.*projects|Link.*projects"
---

<objective>
Refactor project card click behavior so card body navigates to case studies while a dedicated Quick Stats button triggers the flip, and consolidate the footer from 4 columns to 3.

Purpose: The current card flip-on-click obscures navigation to case study pages. Separating concerns makes the interaction intuitive. The footer has a redundant Quick Links column that duplicates navbar.
Output: Refactored ProjectCard with dual interaction, updated ProjectsPage passing slug data, streamlined 3-column Footer.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-page-structure-navigation/11-CONTEXT.md

<interfaces>
<!-- ProjectCard current props (src/components/ProjectCard.tsx): -->
```typescript
interface ProjectCardProps {
  title: string;
  location: string;
  client: string;
  value: string;
  scope: string;
  imageUrl?: string;
  status?: string;
  description?: string;
  systems?: string[];
  highlights?: string[];
  duration?: string;
  sqft?: string;
}
```

<!-- ProjectsPage case study mapping (src/pages/ProjectsPage.tsx line 7-9): -->
```typescript
const caseStudySlugs: Record<string, string> = {
  'Cabrillo Business Park': 'cabrillo-business-park',
};
```

<!-- ProjectsPage card rendering (line 288-300): -->
```tsx
{filteredProjects.map((project) =>
  <div key={project.title} className="flex flex-col">
    <ProjectCard {...project} />
    {caseStudySlugs[project.title] && (
      <Link
        to={`/projects/${caseStudySlugs[project.title]}`}
        className="mt-3 inline-flex items-center gap-1.5 text-sm font-medium text-accent hover:text-accent-dark transition-colors"
      >
        View Case Study <ArrowRight className="h-3.5 w-3.5" />
      </Link>
    )}
  </div>
)}
```

<!-- Footer current structure: 4-column grid (md:grid-cols-4) -->
<!-- Columns: Company Info | Quick Links | Our Services | Contact Us -->
<!-- Bottom bar: copyright + SBE Certified + DIR Registered + Fully Insured -->
<!-- Imports from '../data/credentials': CSLB_LICENSE_DISPLAY, DIR_STATUS -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ProjectCard interaction and ProjectsPage integration</name>
  <files>src/components/ProjectCard.tsx, src/pages/ProjectsPage.tsx</files>
  <action>
**ProjectCard.tsx changes:**

1. Add optional `caseStudySlug` prop to `ProjectCardProps`:
   ```typescript
   caseStudySlug?: string;
   ```

2. Import `useNavigate` from `react-router-dom`.

3. Change the outer container click behavior:
   - Remove `onClick={handleFlip}` from the outer div
   - Remove `role="button"` and `tabIndex={0}` from the outer div (it's no longer a single button)
   - Remove the `handleKeyDown` function
   - If `caseStudySlug` is provided: the card body area (image + content, NOT the Quick Stats button) should navigate to `/projects/${caseStudySlug}` on click. Wrap the front face content in a clickable area (use `onClick` with `navigate()`, keep `cursor-pointer`)
   - If `caseStudySlug` is NOT provided: the card body click triggers `handleFlip()` (preserving current behavior for non-case-study projects)

4. Add a "Quick Stats" button at the bottom of the front face (below the scope row, above the accent line):
   - Always visible (not hover-only), small and understated
   - Use RotateCcw icon (already imported) + text "Quick Stats"
   - Styling: `text-xs text-slate-500 hover:text-accent transition-colors flex items-center gap-1 mt-3 pt-2 border-t border-slate-100`
   - onClick: `(e) => { e.stopPropagation(); handleFlip(); }` — stops propagation so it doesn't trigger navigation
   - `role="button"`, `tabIndex={0}`, proper aria-label

5. Remove the hover "Tap for details" hint overlay (lines ~114-122) — the Quick Stats button replaces this affordance.

6. For case-study cards, add a "View Case Study" link on the front face:
   - Place it next to or near the Quick Stats button
   - Use `ArrowRight` icon (import from lucide-react) + text "View Case Study"
   - Styling: `text-xs text-accent hover:text-accent-dark font-medium flex items-center gap-1`
   - This is a secondary navigation affordance (the whole card body also navigates)
   - onClick: `(e) => { e.stopPropagation(); navigate('/projects/' + caseStudySlug); }`

7. Keep the back face "Flip Back" button and all back face content unchanged.

**ProjectsPage.tsx changes:**

1. Remove the separate `<Link>` element for case study navigation (lines 292-298) — this is now integrated into the card itself.

2. Pass `caseStudySlug` prop to `ProjectCard`:
   ```tsx
   <ProjectCard {...project} caseStudySlug={caseStudySlugs[project.title]} />
   ```

3. The wrapper `<div className="flex flex-col">` can be simplified since the Link below the card is removed. Keep the div for grid layout consistency but remove `flex flex-col` if no longer needed.

4. Remove unused imports: `Link` and `ArrowRight` from ProjectsPage if no longer used elsewhere in the file. Check before removing — `ArrowRight` might be used in the CTA section.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- Project cards with case studies (Cabrillo Business Park) navigate to `/projects/cabrillo-business-park` on body click
- Project cards without case studies flip on body click (existing behavior)
- All cards show a "Quick Stats" button that triggers the flip animation
- Case study cards additionally show a "View Case Study" link on the front face
- The "Tap for details" hover hint is removed
- The separate Link below the card in ProjectsPage is removed
  </done>
</task>

<task type="auto">
  <name>Task 2: Consolidate footer from 4 columns to 3</name>
  <files>src/components/Footer.tsx</files>
  <action>
Restructure Footer.tsx from 4-column to 3-column layout per user decisions:

1. Change grid from `md:grid-cols-4` to `md:grid-cols-3`.

2. **Column 1 — Company** (unchanged):
   - Logo, blurb, CSLB badge — keep exactly as-is

3. **Column 2 — Services** (unchanged):
   - "Our Services" heading + 8 service items with accent dot bullets — keep exactly as-is

4. **Column 3 — Contact & Links** (merged from old columns 2 and 4):
   - Heading: "Contact Us" (keep existing heading)
   - Contact info section: address, phone, email with icons (keep existing markup from old Contact column)
   - Add a divider or spacing after contact info: `<div className="border-t border-slate-700 mt-5 pt-4">`
   - Navigation links section below:
     - Small label (not full heading): `<p className="text-xs uppercase tracking-wider text-slate-500 mb-3">Quick Links</p>`
     - Render the 5 nav links (Home, About Us, Our Projects, Residential Services, Contact) in a compact format
     - Use `text-sm` links with `hover:text-white transition-colors` (same as current Quick Links styling)
     - Use a 2-column grid for compact layout: `grid grid-cols-2 gap-2`

5. **Bottom bar** — keep unchanged: copyright + SBE Certified + DIR Registered + Fully Insured

6. Remove the old standalone "Quick Links" column entirely.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>Footer renders 3 columns: Company (logo + blurb + CSLB), Services (8 items), Contact & Links (contact info + nav links). Bottom bar unchanged. No content lost, just restructured.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- ProjectCard body click navigates to case study for Cabrillo Business Park
- ProjectCard body click flips for all other projects
- Quick Stats button flips card for all projects
- Footer shows 3 columns with nav links folded into Contact column
</verification>

<success_criteria>
- Clicking Cabrillo Business Park card body goes to /projects/cabrillo-business-park
- Clicking any other project card body flips it
- Every card front has a visible "Quick Stats" button
- Footer has exactly 3 columns on desktop
- All existing footer content (services, contact info, nav links) is preserved
</success_criteria>

<output>
After completion, create `.planning/phases/11-page-structure-navigation/11-02-SUMMARY.md`
</output>
