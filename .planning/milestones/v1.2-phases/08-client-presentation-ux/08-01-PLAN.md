---
phase: 08-client-presentation-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ClientLogos.tsx
  - src/components/FloatingCTA.tsx
  - src/App.tsx
  - src/pages/HomePage.tsx
autonomous: true
requirements:
  - DSGN-05
  - UX-01

must_haves:
  truths:
    - "Client logos display in a static grid layout with grayscale-to-color hover effect, not an infinite-scroll marquee"
    - "FloatingCTA on /residential shows 'Request a Quote' linking to /contact?type=residential"
    - "FloatingCTA on all non-residential pages shows 'Request Prequal Package' linking to /contact?type=commercial"
    - "FloatingCTA appears on every page, not just HomePage"
  artifacts:
    - path: "src/components/ClientLogos.tsx"
      provides: "Static grid logo display with grayscale hover effect"
      contains: "grayscale"
    - path: "src/components/FloatingCTA.tsx"
      provides: "Route-aware CTA with residential/commercial variants"
      contains: "useLocation"
    - path: "src/App.tsx"
      provides: "Global FloatingCTA rendering"
      contains: "FloatingCTA"
    - path: "src/pages/HomePage.tsx"
      provides: "HomePage without FloatingCTA (moved to App.tsx)"
  key_links:
    - from: "src/components/FloatingCTA.tsx"
      to: "react-router-dom useLocation"
      via: "pathname check for /residential"
      pattern: "useLocation.*pathname.*residential"
    - from: "src/App.tsx"
      to: "src/components/FloatingCTA.tsx"
      via: "import and render in layout"
      pattern: "FloatingCTA"
---

<objective>
Replace the infinite-scroll client logo marquee with a distinctive static treatment, and make FloatingCTA route-aware so it adapts copy and link targets between residential and commercial pages.

Purpose: Complete the v1.2 GC Appeal & Design Polish milestone by giving client logos a professional B2B presentation and ensuring the primary CTA is contextually relevant on every page.
Output: Redesigned ClientLogos component, route-aware FloatingCTA rendered globally from App.tsx.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-client-presentation-ux/08-CONTEXT.md
@src/components/ClientLogos.tsx
@src/components/FloatingCTA.tsx
@src/App.tsx
@src/pages/HomePage.tsx

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/components/FloatingCTA.tsx:
```typescript
export function FloatingCTA(): React.JSX.Element
// Currently takes no props — will need useLocation added internally
```

From src/components/ui/Button.tsx:
```typescript
// Button with variant and size props, already used in FloatingCTA
```

From src/App.tsx layout structure:
```
ErrorBoundary > Router > ScrollToHash > div > Navbar + main(Routes) + Footer
// FloatingCTA must be inside Router (needs useLocation) but outside Routes
```

Client data array (keep as-is):
```typescript
const clients: { name: string; logo: string; invert?: boolean }[] = [
  { name: 'A&R Construction', logo: '/images/clients/a-and-r-construction.avif' },
  // ... 10 clients total
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign ClientLogos with static grid and grayscale hover</name>
  <files>src/components/ClientLogos.tsx</files>
  <action>
Rewrite the ClientLogos component render logic. Keep the existing `clients` data array unchanged.

Remove entirely:
- The `doubledClients` array doubling
- The marquee scrolling track div with `animate-marquee` class
- The gradient mask divs
- The `<style>` block with `@keyframes marquee` CSS

Replace with a static grid layout:
- Use a responsive CSS grid: 2 columns on mobile, 3 on sm, 5 on lg (to fit all 10 logos in 2 rows)
- Each logo cell: centered, with consistent padding, a subtle border or card treatment (e.g. `border border-slate-200 rounded-lg p-6`)
- Logo images: `grayscale` filter by default via Tailwind `grayscale` class, transitioning to full color on hover with `hover:grayscale-0`
- Keep the `invert` flag behavior for Walton Construction
- Keep opacity behavior: `opacity-60` default, `hover:opacity-100` with smooth transition
- Add `transition-all duration-300` for hover effects
- Show client name as small text label below each logo: `text-xs text-slate-400 mt-2`

Section wrapper:
- Use a SectionHeader or simple heading that fits the industrial brand. A centered uppercase subheading like "Trusted By" in `text-sm font-semibold text-accent uppercase tracking-wider` works well.
- Section background: `bg-slate-50` to differentiate from adjacent white sections
- Keep the `py-12` or similar vertical padding

The section should feel like a professional "Our Clients" grid you'd see on a B2B services site — clean, static, with the grayscale effect adding visual sophistication.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ClientLogos renders a static responsive grid of 10 logos with grayscale-to-color hover effect, client name labels, no marquee animation, no scrolling behavior</done>
</task>

<task type="auto">
  <name>Task 2: Make FloatingCTA route-aware and move to App.tsx global layout</name>
  <files>src/components/FloatingCTA.tsx, src/App.tsx, src/pages/HomePage.tsx</files>
  <action>
**FloatingCTA.tsx changes:**

1. Add `import { useLocation } from 'react-router-dom';` (Link is already imported)
2. Inside the component, call `const { pathname } = useLocation();`
3. Derive a boolean: `const isResidential = pathname === '/residential';`
4. Replace hardcoded text with conditional content:
   - Headline: `isResidential ? "Ready for your project?" : "Ready to prequalify?"`
   - Subtext: `isResidential ? "Showers, mirrors, railings & more" : "Get COI, EMR, and references within 24 hours"`
   - Button text (desktop): `isResidential ? "Request a Quote" : "Request Prequal Package"`
   - Button text (mobile): `isResidential ? "Get Quote" : "Get Prequal"`
   - Link `to` prop: `isResidential ? "/contact?type=residential" : "/contact?type=commercial"`
5. Keep ALL existing behavior unchanged: scroll thresholds, dismiss logic, brand navy styling, icon, layout structure

**App.tsx changes:**

1. Add `import { FloatingCTA } from './components/FloatingCTA';`
2. Render `<FloatingCTA />` inside the Router but outside Routes — place it after `<Footer />` and before the closing `</div>`, so the DOM order is: Navbar, main(Routes), Footer, FloatingCTA. This is correct because FloatingCTA is `fixed` positioned and won't affect layout flow.

**HomePage.tsx changes:**

1. Remove the `import { FloatingCTA } from '../components/FloatingCTA';` line (line 14)
2. Remove the `<FloatingCTA />` JSX render (line 524)
3. Ensure no other references to FloatingCTA remain in the file
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearsite && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>FloatingCTA renders globally from App.tsx on all pages, shows "Request a Quote" with residential link on /residential, shows "Request Prequal Package" with commercial link on all other pages, HomePage no longer imports or renders FloatingCTA</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Dev server (`npm run dev`): Navigate to HomePage — FloatingCTA appears after scrolling 500px with commercial copy
3. Navigate to /residential — FloatingCTA shows "Ready for your project?" / "Showers, mirrors, railings & more" / "Request a Quote" button
4. Navigate to /about, /projects, /contact — FloatingCTA shows commercial copy on all of them
5. ClientLogos section on HomePage shows a static grid of logos in grayscale, hovering turns them to color
6. No infinite scroll animation present anywhere
</verification>

<success_criteria>
- Client logos render in a static grid with grayscale default and color-on-hover, zero marquee behavior
- FloatingCTA appears on every page after 500px scroll
- /residential route gets residential copy and /contact?type=residential link
- All other routes get commercial copy and /contact?type=commercial link
- Build passes with no TypeScript or bundling errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-client-presentation-ux/08-01-SUMMARY.md`
</output>
