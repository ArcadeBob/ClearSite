---
phase: 01-portfolio-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ProjectsPage.tsx
autonomous: true
requirements:
  - PORT-01
  - PORT-02

must_haves:
  truths:
    - "Every project that previously displayed 'Current' status now displays 'Completed'"
    - "Every project that previously displayed 'Future' status now displays 'Current'"
    - "The Projects page filter tabs correctly surface projects under their updated labels"
    - "No TypeScript errors are introduced by the status changes"
  artifacts:
    - path: "src/pages/ProjectsPage.tsx"
      provides: "Updated project status data for all 17 projects"
      contains: "status: 'Completed' as const"
  key_links:
    - from: "src/pages/ProjectsPage.tsx (projects array)"
      to: "ProjectCard component status prop"
      via: "status field passed as prop"
      pattern: "status: '(Completed|Current)' as const"
---

<objective>
Update all project status values in ProjectsPage.tsx so the portfolio accurately reflects current reality: previously-current projects are now completed, and previously-future projects are now in progress.

Purpose: The hardcoded project data has stale status labels. General contractors visiting the portfolio see incorrect project states.
Output: Updated `src/pages/ProjectsPage.tsx` with 7 status string edits (4 Current->Completed, 3 Future->Current).
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-portfolio-status/01-RESEARCH.md
@src/pages/ProjectsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update project status strings in ProjectsPage.tsx</name>
  <files>src/pages/ProjectsPage.tsx</files>
  <action>
Edit the `projects` array in `src/pages/ProjectsPage.tsx` to update 7 status values. Use the Edit tool for each change — do NOT use find-and-replace across the whole file since `'Current'` appears in entries that need different target values.

**PORT-01 — Change 4 projects from `'Current'` to `'Completed'`:**

1. Line 18 — Citrus Commons: `status: 'Current' as const` -> `status: 'Completed' as const`
2. Line 27 — 9900 Venice: `status: 'Current' as const` -> `status: 'Completed' as const`
3. Line 135 — 4750 Santa Monica: `status: 'Current' as const` -> `status: 'Completed' as const`
4. Line 144 — Jordan Downs S4: `status: 'Current' as const` -> `status: 'Completed' as const`

**PORT-02 — Change 3 projects from `'Future'` to `'Current'`:**

5. Line 36 — Via Avanti: `status: 'Future' as const` -> `status: 'Current' as const`
6. Line 153 — Wilshire Lofts: `status: 'Future' as const` -> `status: 'Current' as const`
7. Line 162 — 1st Street North: `status: 'Future' as const` -> `status: 'Current' as const`

**IMPORTANT:** Each edit must use sufficient surrounding context (include the project name from the line above) to ensure the correct entry is targeted, since `status: 'Current' as const` appears multiple times.

**Do NOT change:**
- The TypeScript union type in `ProjectCard.tsx` (keep `'Completed' | 'Current' | 'Future'`)
- The filter tab definitions or filter logic
- Any other field in any project entry
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearSite && grep -c "status: 'Current' as const" src/pages/ProjectsPage.tsx | grep -q "^3$" && grep -c "status: 'Future' as const" src/pages/ProjectsPage.tsx | grep -q "^0$" && echo "PASS: 3 Current, 0 Future" || echo "FAIL: Expected 3 Current and 0 Future"</automated>
  </verify>
  <done>
- Zero projects have `status: 'Future' as const` in ProjectsPage.tsx
- Exactly 3 projects have `status: 'Current' as const` (Via Avanti, Wilshire Lofts, 1st Street North)
- All remaining projects (14) have `status: 'Completed' as const`
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build succeeds and status counts are correct</name>
  <files>src/pages/ProjectsPage.tsx</files>
  <action>
Run the TypeScript/Vite build to confirm no type errors were introduced by the status changes. Then run grep verification to confirm final status distribution.

1. Run `npm run build` from the project root — must succeed with zero errors.
2. Run `grep -n "status:" src/pages/ProjectsPage.tsx` — confirm the full list of 17 status lines shows exactly 14 Completed + 3 Current + 0 Future.
3. Confirm no changes to `ProjectCard.tsx` by running `git diff src/components/ProjectCard.tsx` — should be empty.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearSite && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- `npm run build` exits 0 with no TypeScript errors
- Final status distribution: 14 Completed, 3 Current, 0 Future
- ProjectCard.tsx is unchanged
  </done>
</task>

</tasks>

<verification>
1. `grep "status: 'Current' as const" src/pages/ProjectsPage.tsx` returns exactly 3 lines (Via Avanti, Wilshire Lofts, 1st Street North)
2. `grep "status: 'Future' as const" src/pages/ProjectsPage.tsx` returns 0 lines
3. `grep "status: 'Completed' as const" src/pages/ProjectsPage.tsx` returns 14 lines
4. `npm run build` completes with exit code 0
5. `git diff src/components/ProjectCard.tsx` is empty
</verification>

<success_criteria>
- PORT-01 satisfied: All 4 previously-Current projects (Citrus Commons, 9900 Venice, 4750 Santa Monica, Jordan Downs S4) now show Completed
- PORT-02 satisfied: All 3 previously-Future projects (Via Avanti, Wilshire Lofts, 1st Street North) now show Current
- No TypeScript errors, no regressions, no changes outside ProjectsPage.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/01-portfolio-status/01-01-SUMMARY.md`
</output>
